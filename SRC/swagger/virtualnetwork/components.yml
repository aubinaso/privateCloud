type: "object"
required:
  - "name"
  - "network"
properties:
  name:
    type: "string"
  mode:
    type: "integer"
    description: "nat, route, open, bridge, private, passthrough, hostdev, vepa, "
  network:
    type: "string"
  tag:
    type: "array"